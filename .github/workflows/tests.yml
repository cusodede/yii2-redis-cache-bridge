name: Run tests
on:
  pull_request:
    branches: [ master ]
jobs:
  codeception:
    name: PHP ${{ matrix.php }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: [ 80, 81 ]
    steps:
      - name: Checkout
        uses: actions/checkout@v2